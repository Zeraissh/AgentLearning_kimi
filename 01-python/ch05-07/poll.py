"""练习 8：小调查（while + input + 字典）。

任务：做一个命令行小调查——反复询问"你最喜欢的编程语言是什么？"：
    1. 用 while 循环持续接收输入
    2. 输入 q 退出循环
    3. 用字典统计每种语言被提到的次数（提示：dict.get(lang, 0) + 1）
    4. 退出后打印统计结果，按次数从高到低排序

示例运行：
    你最喜欢的编程语言是什么？(输入 q 退出) python
    你最喜欢的编程语言是什么？(输入 q 退出) rust
    你最喜欢的编程语言是什么？(输入 q 退出) python
    你最喜欢的编程语言是什么？(输入 q 退出) q
    --- 统计结果 ---
    python: 2 票
    rust: 1 票

提示：排序用 sorted(results.items(), key=lambda x: x[1], reverse=True)，
     lambda 看不懂先照抄，函数那章会讲
"""

# 在下面写你的代码：


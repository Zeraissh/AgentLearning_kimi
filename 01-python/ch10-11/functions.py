"""练习 12：给你的代码写测试（pytest）。

任务：
    1. 把练习 9 的 total() 函数复制到本文件
    2. 在 test_functions.py 里为它写至少 3 个测试：
       - 常规情况：total(1, 2, 3) == 6
       - 边界情况：total() == 0
       - 负数情况：total(-1, 1) == 0
    3. 运行 pytest 01-python/ch10-11/ 确认全绿
    4. 故意把一个断言改错，再跑一次，观察失败报告长什么样——
       学会读测试失败报告和学会写测试一样重要，看完改回来

提示：
    - 测试文件名必须是 test_ 开头，测试函数也必须 test_ 开头
    - 没装 pytest 的话先 pip install pytest（记得在虚拟环境里）
"""

# 在下面写你的代码（total 函数）：

